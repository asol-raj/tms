<div class="container-lg">
    <div class="row mt-2 py-2 align-items-center">
        <div class="col-12 col-sm-3 mb-3 mb-sm-0">
            <span class="fw-bold mb-0 fs-1 text-primary nav-link">TMS</span>
            <span class="text-secondary"> <?= user.fullname ?> </span>
        </div>

        <div class="col-12 col-sm-9">
            <div class="d-flex justify-content-end align-items-center gap-5">

                <a href="/auth/daily/tasks" class="btn btn-bd-outline-primary px-sm-4 border-2 me-auto me-sm-0" title="View Daily Tasks List">DAILY TASKS</a>
                <? if(user.role !== 'user'){ ?>
                <button class="btn btn-bd-primary px-sm-5 create-task border-2" title="Create A New Task"><i class="bi bi-plus-lg"></i> TASK</button>
                <? } ?>

                

                <div class="dropdown">
                    <div class="d-flex justify-content-center align-items-center dropdown-toggle"
                        data-bs-toggle="dropdown" aria-expanded="false">
                        <span class="fs-2 user-avatar rounded-circle d-flex justify-content-center align-items-center"
                            style="width: 34px; height: 34px; font-size: 0.9rem;">
                            <i class="bi bi-person-fill"></i>
                        </span>
                    </div>

                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><span class="dropdown-item" role="button" id="updateProfile">Profile</span></li>
                        <li><span class="dropdown-item" role="button" id="changePwd">Change Password</span></li>
                        <li><span class="dropdown-item disabled" role="button" id="viewPosts">Show Posts</span></li>
                        <li><a class="dropdown-item" href="/auth/daily/tasks">Daily Tasks</a></li>

                        <? if(user.role === 'admin'){ ?>
                        <li><a class="dropdown-item" href="/auth/settings">Settings</a></li>
                        <? } ?>

                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<hr>
<div class="container-fluid">
    <div class="row g-4">
        <div class="col-9 border-end" id="leftSide">
            <div class="my-4 d-flex flex-wrap justify-content-between align-items-center gap-5 filter-buttons"
                id="actionBtn">
                <button class="btn btn-success px-lg-5 border-2 me-auto filter-btn fw-semibold all">All</button>
                <button class="btn btn-outline-info px-lg-5 border-2 filter-btn fw-semibold pending">Pending</button>
                <button class="btn btn-outline-primary px-lg-5 border-2 filter-btn fw-semibold in_progress">In
                    Progress</button>
                <button
                    class="btn btn-outline-success px-lg-5 border-2 filter-btn fw-semibold completed">Completed</button>
                <button
                    class="btn btn-outline-danger px-lg-5 border-2 filter-btn fw-semibold archived d-none">Archived</button>
            </div>

            <div class="dataTable table-responsive" id="dataTable"></div>

        </div>
        <!-- rgb(184, 255, 251) -->
        <div class="col-3 updates bg-light-subtle" id="rightSide">
            <div class="postsfeeds">
                <div class="d-flex justify-content-between align-items-center position-relative">
                    <h4 class="mb-0">Posts & Feeds</h4>
                    <button class="btn-close" id="togglePosts" title="Hide Posts & Feeds"></button>
                    <!-- <p>Scroll down to see the floating button.</p> -->
                </div>

                <div id="createPostContainer" class="posts my-3 p-3 border rounded-2 d-none">
                    <h5>Create a New Post</h5>
                    <hr class="my-3">

                    <!-- Note: I fixed the ID from 'creaetPost' to 'createPost' -->
                    <form id="createPost">
                        <textarea name="post" id="postMsg" class="form-control mb-3" rows="4"
                            placeholder="Post New Updates Here"></textarea>

                        <!-- Button wrapper for side-by-side buttons -->
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary w-100">Post</button>
                            <!-- This 'Cancel' button will also hide the form -->
                            <button type="button" id="cancelPostBtn" class="btn btn-secondary w-100">Cancel</button>
                        </div>
                    </form>
                </div>

                <!-- This is the 'view-posts' div from your other code -->
                <div class="view-posts mt-4">
                    <!-- Your loaded posts will go here -->
                </div>

            </div>

            <? if(user.role !== 'user'){ ?>
            <button id="showPostFormBtn" class="btn btn-primary btn-lg rounded-circle position-fixed bottom-0 end-0 m-4"
                type="button" title="Show Create New Post">
                <i class="bi bi-plus-lg"></i>
            </button>
            <? } ?>
        </div>
    </div>
</div>

<!-- filter keys template-->
<div id="filterDropdown" class="position-absolute border bg-white p-2 rounded shadow small d-print-none"
    style="display:none; z-index:1050; width:250px; max-height:300px;" tabindex="-1">
    <input type="text" class="form-control form-control-sm mb-2" id="filterSearch" placeholder="Search..." />
    <div id="filterItems" class="overflow-auto" style="max-height:180px;"></div>
    <div class="d-flex justify-content-between gap-1 mt-2">
        <button class="btn btn-sm btn-outline-secondary me-auto" id="clearFilter">Clear</button>
        <button class="btn btn-sm btn-outline-secondary" id="applyFilter">Apply</button>
        <button class="btn btn-sm btn-outline-secondary" id="cancelFilter">Cancel</button>
    </div>
</div>

<style>
    /* .filter-buttons .filter-btn.active {
        transform: scale(1.05);
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        transition: all 0.15s ease-in-out;
    } */

    #showPostFormBtn {
        /* position: fixed; */
        /* Floats relative to the viewport */
        /* bottom: 30px; */
        right: 30px;
        width: 60px;
        /* Round button dimensions */
        height: 60px;
        font-size: 20px;
        /* Makes the '+' bigger */
        line-height: 1.5;
        z-index: 1050;
        /* Ensures it's on top of other content */
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);

        /* Prevents text selection on double click */
        user-select: none;
    }

    /* --- Default State (Sidebar Visible) --- */

    #leftSide {
        /* Your default 'col-9' styles */
        width: 75%;
        transition: width 0.3s ease;
        /* Bonus: nice animation */
    }

    #rightSide {
        /* Your default 'col-3' styles */
        width: 25%;
        display: block;
        transition: all 0.3s ease;
    }

    /* --- Toggled State (Sidebar Hidden) --- */

    body.sidebar-hidden #leftSide {
        /* Your 'col-12' styles */
        width: 100%;
    }

    body.sidebar-hidden #rightSide {
        display: none;
    }
</style>

<script src="/_libs/flatpickr.js"></script>
<script type="module" src="/js/dashboard.js"></script>

<!-- <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script> -->
<script src="/_libs/jquery/jquery.js"></script>
<script>
    // Alias jQuery to 'jq' to match your code style
    const jq = jQuery.noConflict();
    // jq('#hidePosts').on('click', () => {
    //     jq("#rightSide").addClass('d-none');
    //     jq('#leftSide').removeClass('col-9').addClass('col-12');
    //     jq('#viewPosts').removeClass('disabled');
    // })

    // jq('#viewPosts').on('click', () => {
    //     jq('#leftSide').removeClass('col-12').addClass('col-9');
    //     jq("#rightSide").removeClass('d-none');
    //     jq('#viewPosts').addClass('disabled');
    // })

    jq('#togglePosts, #viewPosts').on('click', () => {
        jq('body').toggleClass('sidebar-hidden');
        jq('#viewPosts').toggleClass('disabled');
    });

</script>